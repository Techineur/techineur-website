'use client'; // This is a client component 👈🏽

import React from 'react';
import { Features } from 'views/Services/Components';

const feature = (): JSX.Element => {
  return <Features />;
};

export default feature;
